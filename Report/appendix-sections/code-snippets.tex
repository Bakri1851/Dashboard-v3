\section{Code Snippets}

% =====================================================================
% TODO (Bakri): READ-THROUGH PASS before final submission.
%
% Appendix A is currently a heading only.  Populate with key code
% excerpts that support the Ch4 implementation narrative.  Suggested
% snippets, in order:
%   - config.py threshold tuples (struggle / difficulty) - the
%     \label{sec:thresholds-struggle} / \label{sec:thresholds-difficulty}
%     in implementation.tex already point readers near these constants
%   - config.py composite weight vectors (struggle / difficulty)
%   - scripts/eval_label.py LLM rater prompt + 4-band schema
%   - scripts/optimise_v2_weights.py OLS fit core
%     (fit_struggle_weights / fit_difficulty_weights)
%   - scripts/threshold_search_constrained.py grid-search inner loop
%   - scripts/threshold_search_v2composite.py deployed-composite recal
%     (the pass referenced in §5.4.5 deployed-cutpoints paragraph)
%   - analytics.py incorrectness scorer fallback path (the midpoint
%     fallback referenced in §5.6.3 limitations)
%
% Use the thesisPython lstlisting style already defined in main.tex
% L43-63 (basicstyle=\footnotesize\ttfamily, single frame, line
% numbers).  Caption each listing with the source file and a one-line
% purpose so the reader can locate the live code.
% =====================================================================