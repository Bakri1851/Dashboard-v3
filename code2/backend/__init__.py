"""FastAPI backend serving the code2 alternative frontend.

Imported module-path: `backend.*` when uvicorn runs with `--app-dir code2`.
Shared analytics / models / state all live in `learning_dashboard/` alongside.
"""
