# Ch5 §5.2 — Functional Testing Drafting Plan

Companion to [[Ch5 – Results and Evaluation]]. Built 2026-05-26 as the next phase of the natural-order Ch5 completion plan after the v2 target-swap reconciliation closed. Mirrored from the working plan at `~/.claude/plans/i-m-continuing-thesis-work-ticklish-waffle.md`.

## Context

§5.2 reports the **functional verification** of the deployed V2 stack against the functional requirements (FR1–FR7) of Chapter~\ref{sec:requirements}. Per the body-vs-appendix split adopted on 2026-05-24, the body carries the qualitative summary and any items that failed or required workaround; the full FR1–FR7 row-by-row checklist (steps, expected, actual, evidence pointer) lives in **Appendix~\ref{app:detailed-test-results}** (currently empty stub; user will populate). Screenshots referenced from the body live in **Appendix~\ref{app:ui-screenshots}** (also currently empty).

The §5.2 subsubsections track the requirement clusters of Ch2 §2.7 Requirements Specification:

- §5.2.1 → FR1 (live data ingestion + session handling)
- §5.2.2 → FR2 (struggle metrics) + FR3 (difficulty metrics)
- §5.2.3 → FR4 (prioritisation) + FR5 (analytics presentation)
- §5.2.4 → FR7 (lab-assistant ranking + assignment); FR6 (smart-device integration) is documented here as scoped-out / future work

FR6 was scoped out at design time (the responsive lab-assistant SPA on a phone is the closest deployed surrogate; native smart-device integration was not implemented).

## Voice / convention notes

- Long sentences joined by commas and semicolons; 2–3 clauses per sentence is fine
- British spelling, "tradeoff" not "trade-off"
- Causal "as" or "due to", not "because"
- Each subsubsection closes with a one-line pointer to Appendix~\ref{app:detailed-test-results} for the per-FR detail rows

## Cross-refs the prose uses (verify in build)

- `\ref{sec:requirements}` — Ch2 chapter label (FRs + NFRs live in Ch2 §2.7); added 2026-05-26 to fix the §5.2 prose blocks that previously pointed `\ref{sec:design}` at the FRs
- `\ref{sec:design}` — Ch3 chapter label ✓ (used for design equations and model formulas only, not FRs)
- `\ref{sec:implementation}` — Ch4 chapter label ✓
- `\ref{sec:pipeline}` — Ch4 §4.4 Data Pipeline ✓ (confirmed in Ch4 grep)
- `\ref{sec:shared-state}` — Ch4 §4.3.4 Shared Runtime State ✓
- `\ref{sec:views}` — Ch4 §4.9 Instructor System Views ✓
- `\ref{sec: student struggle}` — Ch3 §3.3.1 Struggle Model ✓
- `\ref{sec:improved-struggle}` — Ch4 §4.7.4 Improved Struggle Model — **check spelling** (Ch4 uses `\label{sec: improved struggle}` with space at some places; verify)
- `\ref{sec:assistant}` — Ch4 §4.10 Lab Assistant System ✓
- `\ref{sec:eval-discussion}` — Ch5 §5.6 (added in ToC restructure) ✓
- `\ref{app:detailed-test-results}` — Appendix C; needs `\label{app:detailed-test-results}` on the empty file
- `\ref{app:ui-screenshots}` — Appendix B; needs `\label{app:ui-screenshots}` on the empty file

---

## Edits to apply to results-and-evaluation.tex

### §5.2.1 Data Ingestion and Session Handling (~140 words)

**Anchor**: existing `% TODO: 5.2.1 prose block` marker (line 29 of [`results-and-evaluation.tex`](../../../../Report/main-sections/results-and-evaluation.tex)).

```latex
The first functional requirement of Chapter~\ref{sec:requirements} (FR1, live submission-data ingestion) was exercised by running the V2 backend against a live session and confirming that the newline-delimited JSON payload returned by the Loughborough submission API is parsed end-to-end into the normalised submissions DataFrame, that the XML field embedded in each record is extracted into per-submission attributes, and that the in-memory cache invalidates correctly under the configured TTL described in Section~\ref{sec:pipeline}. Session start and session end were exercised through the instructor sidebar and the lab-assistant join flow concurrently, confirming that the shared state file at \texttt{data/lab\_session.json} updates atomically under three-process contention through the \texttt{filelock} exclusive-lock pattern of Section~\ref{sec:shared-state}. No data loss or corruption was observed across the test runs. The full FR1 verification row appears in Appendix~\ref{app:detailed-test-results}.
```

### §5.2.2 Analytics and Model Behaviour (~150 words)

**Anchor**: existing `% TODO: 5.2.2 prose block` marker.

```latex
Functional requirements FR2 (struggle metrics) and FR3 (question difficulty metrics) of Chapter~\ref{sec:requirements} were exercised against the analytical pipeline of Chapter~\ref{sec:implementation}. The seven-signal baseline struggle model and the five-signal baseline difficulty model were confirmed to produce scores in $[0, 1]$ across the cohort, with the four band-classification labels propagating correctly under both the v1 and v2 weight regimes. The collaborative-filtering layer, the mistake-clustering pipeline, the IRT 2PL fit, the BKT mastery estimates, and the improved-struggle composite were each invoked end-to-end and the outputs cross-checked against the design equations of Chapter~\ref{sec:design}; the graceful weight-redistribution cases described in Section~\ref{sec: improved struggle} (BKT unavailable, IRT unavailable, both unavailable) were each exercised with synthetic-cohort fixtures and confirmed to return the expected reweighted scores. No silent failures were observed. The per-model verification rows appear in Appendix~\ref{app:detailed-test-results}.
```

### §5.2.3 Instructor Dashboard Features (~155 words)

**Anchor**: existing `% TODO: 5.2.3 prose block` marker.

```latex
FR4 (prioritisation through ranked leaderboards) and FR5 (analytics presentation through multiple views) of Chapter~\ref{sec:requirements} were exercised through the V2 React stack. Each of the eight instructor views described in Section~\ref{sec:views} was visited in turn, with the rendered components cross-checked against the design specification: the In-Class leaderboards rank by score under both Basic and Advanced layout toggles, the band colour-coding tracks the threshold-classification map of Section~\ref{sec: student struggle}, and the Student Detail, Question Detail, Mistake Clusters, Data Analysis, Session Progression, Previous Sessions, and Settings views render without error on a representative session. Cross-view navigation was tested through the click-through routes that bind a selected student or question to the next view, and the sidebar filter state was confirmed to persist correctly across view changes. Screenshots for each view appear in Appendix~\ref{app:ui-screenshots}; the full FR4 and FR5 verification rows are in Appendix~\ref{app:detailed-test-results}.
```

### §5.2.4 Lab Assistant Workflow (~155 words)

**Anchor**: existing `% TODO: 5.2.4 prose block` marker.

```latex
FR7 (lab-assistant ranking and assignment) of Chapter~\ref{sec:requirements} was exercised through the V2 lab-assistant single-page application. The join flow was tested with the six-character session code described in Section~\ref{sec:assistant}, confirming that the assistant's identity persists across reloads through the \texttt{?aid=} URL parameter and that the five-second polling cycle picks up new assignments without manual refresh. The mark-helped round-trip was exercised end-to-end, confirming that a button press on the assistant device updates \texttt{data/lab\_session.json} through the same file-locked pattern as the instructor process, and that the instructor sidebar refreshes to show the joined assistants and their assignments within one polling cycle. Concurrent join from two assistants was tested without data loss. FR6 (native smart-device integration beyond the responsive lab-assistant SPA) was scoped out at design time and remains future work; this limitation is restated in Section~\ref{sec:eval-discussion}. The full FR7 row appears in Appendix~\ref{app:detailed-test-results}.
```

---

## Figures and tables to include

The body-vs-appendix split keeps §5.2 prose text-only — no inline figures. All visuals live in two appendices that the §5.2 prose cross-refs.

### §5.2 body — no inline figures

§5.2.1, §5.2.2, §5.2.3, §5.2.4 are all qualitative summary paragraphs ending with a pointer to Appendix C (`app:detailed-test-results`); §5.2.3 additionally points to Appendix B (`app:ui-screenshots`).

### Appendix B — `app:ui-screenshots` (10 screenshots; capture pass not yet done)

Add `\label{app:ui-screenshots}` at the top of [`Report/appendix-sections/ui-screenshots.tex`](../../../../Report/appendix-sections/ui-screenshots.tex). Then organise the appendix into two `\subsection{}`s: V2 Instructor Views (eight screenshots) and V2 Lab Assistant SPA (two screenshots).

| # | View / screen | Suggested figure label | Source file (frontend) |
|---|---|---|---|
| 1 | In-Class — Basic layout | `fig:appB-inclass-basic` | `code2/frontend/src/views/InClassView.tsx` |
| 2 | In-Class — Advanced layout | `fig:appB-inclass-advanced` | same |
| 3 | Student Detail | `fig:appB-student-detail` | `code2/frontend/src/views/StudentDetailView.tsx` |
| 4 | Question Detail | `fig:appB-question-detail` | `code2/frontend/src/views/QuestionDetailView.tsx` |
| 5 | Mistake Clusters | `fig:appB-mistake-clusters` | `code2/frontend/src/views/MistakeClustersView.tsx` |
| 6 | Data Analysis | `fig:appB-data-analysis` | `code2/frontend/src/views/DataAnalysisView.tsx` |
| 7 | Session Progression | `fig:appB-session-progression` | `code2/frontend/src/views/SessionProgressionView.tsx` |
| 8 | Previous Sessions | `fig:appB-previous-sessions` | `code2/frontend/src/views/PreviousSessionsView.tsx` |
| 9 | Settings (with v2 weight toggles) | `fig:appB-settings` | [`code2/frontend/src/views/SettingsView.tsx`](../../../../code2/frontend/src/views/SettingsView.tsx) |
| 10a | Lab Assistant SPA — join screen | `fig:appB-assistant-join` | `code2/frontend/src/views/assistant/...` |
| 10b | Lab Assistant SPA — assignment view | `fig:appB-assistant-assignment` | same |

**Capture instructions**: run `uvicorn backend.main:app --app-dir code2 --port 8000` against a representative session; save each screenshot at ≥200 DPI as PNG to `Report/figures/appendix-b/`; crop OS chrome. Estimated 30–60 minutes.

**Paste-ready `\includegraphics` for Appendix B** (one per screenshot):

```latex
\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\linewidth]{figures/appendix-b/<filename>.png}
    \caption{<view name>: <one-sentence purpose>.}
    \label{<fig:appB-* label>}
\end{figure}
```

### Appendix C — `app:detailed-test-results` (FR row-by-row table)

Add `\label{app:detailed-test-results}` at the top of [`Report/appendix-sections/detailed-test-results.tex`](../../../../Report/appendix-sections/detailed-test-results.tex). Then author **`tab:fr-checklist`** — a 7-row table covering FR1–FR7, one row per FR, columns: FR ID / Steps / Expected / Actual / Evidence pointer.

The Appendix C file also receives `tab:nfr-checklist` from §5.3 (NFR1–NFR6, same column structure) — both tables sit under §5.2's `app:detailed-test-results` label.

**Source**: smoke-test pass run against the deployed V2 stack. Each row is one FR + the observed behaviour against the expected behaviour described in Chapter~\ref{sec:requirements}. Evidence pointer can be a screenshot label from Appendix B (e.g. `\ref{fig:appB-inclass-basic}`) or a code reference (`code2/backend/main.py:144`).

---

## Pre-paste checklist for the user

1. Verify the Ch4 label `\label{sec: improved struggle}` (with space) vs `\label{sec:improved-struggle}` (without). The drafting plan uses the space-containing form per the explore-agent inventory; if your live Ch4 uses the no-space form, adjust the Ch5 §5.2.2 cross-ref.
2. Add `\label{app:detailed-test-results}` near the top of [`Report/appendix-sections/detailed-test-results.tex`](../../../../Report/appendix-sections/detailed-test-results.tex) (currently empty) so the four §5.2 references resolve.
3. Add `\label{app:ui-screenshots}` near the top of [`Report/appendix-sections/ui-screenshots.tex`](../../../../Report/appendix-sections/ui-screenshots.tex) (currently empty) so §5.2.3 resolves.
4. After pasting all four §5.2 blocks, run `pdflatex Report/main.tex` twice and check the log for `Reference ... undefined` warnings on the appendix labels.

## Post-paste audit checklist (for me)

1. Confirm British spelling, "tradeoff" not "trade-off"
2. Confirm Ch4 §4.6–§4.11 voice (long sentences with commas/semicolons)
3. Confirm no V1/V2 conflations — the §5.2 prose refers to V2 React stack explicitly for the workflows that are V2-only (instructor views, lab-assistant SPA)
4. Confirm cross-refs to Ch3 / Ch4 / appendices resolve in the build
